# **4. Глубокие модули (Интерфейсы должны быть простыми, как палка)**

Самый важный скилл в борьбе со сложностью софта — это проектировать систему так, чтобы разработчик в любой момент времени **охуевал только от маленького кусочка**, а не от всей кучи говна сразу. Этот подход называется модульным дизайном. Сейчас поясню за базу.

## **4.1 Модульный дизайн: Разделяй и властвуй**

В нормальной модульной системе софт нарезается на относительно независимые куски. Это могут быть классы, подсистемы, сервисы — называй как хочешь. В идеальном мире (где живут единороги и проджект-менеджеры с работающим мозгом) каждый модуль полностью независим. Ты ковыряешь свой кусок и вообще не ебешь, что там у соседа. В таком мире сложность системы равна сложности самого херового модуля.

Но, к сожалению, мы живем в реальности. Модули должны вызывать друг друга, чтобы хоть что-то работало. Значит, **модули должны друг о друге знать. Появляются зависимости**. Если ты поменял одну херню в модуле А, модуль Б может смачно дрыснуть. Например, поменял аргументы функции — и теперь бегай по всему проекту, переписывай вызовы, потому что компилятор орет. Зависимости бывают хитрые и незаметные, как баг в пятницу вечером. Цель модульного дизайна — **свести эти зависимости к минимуму**, чтобы модули не трахали друг другу мозги.

Чтобы это разрулить, делим модуль на две части: **Интерфейс** и **Реализация**.

*   **Интерфейс** — это то, что *должен* знать другой разраб, чтобы юзать твой код. Это "ЧТО" модуль делает.
*   **Реализация** — это "КАК" он это делает. Те самые кишки, которые ты прячешь.

Разработчик должен знать интерфейс и реализацию *своего* модуля, но от *чужих* модулей ему нужен только интерфейс. Ему должно быть глубоко насрать, как там у тебя внутри всё устроено.

Возьмём, например, модуль сбалансированного дерева. Внутри там может быть лютый матан, повороты узлов и черная магия. Но юзеру модуля похер. Он видит простой интерфейс: `insert`, `delete`, `get`. Он сует ключ и значение, а как там дерево будет расщепляться — не его собачье дело.

В этой книге "модуль" — это кусок кода с интерфейсом и реализацией. В ООП это класс. Лучшие модули — это те, где **интерфейс в разы проще реализации**. У этого два плюса:
1.  Ты не грузишь других разрабов лишней инфой.
2.  Ты можешь переписать свои говнокодные кишки (реализацию), не ломая код остальным, пока интерфейс остается прежним.

## **4.2 Чё там внутри интерфейса?**

Интерфейс состоит из двух частей: формальной и неформальной.
*   **Формальная часть**: То, что понимает компилятор. Сигнатуры методов, типы данных, эксепшены. Тут язык программирования бьет тебя по рукам, если ты накосячил.
*   **Неформальная часть**: То, что компилятор не выкупает, но разраб знать обязан. Например, что функция "удаляет файл к херам" или "этот метод надо вызывать только после того". Обычно это пишется в комментах (если тебе не лень). И вот тут самая жопа: неформальная часть часто сложнее и важнее формальной.

Четкий интерфейс избавляет от проблемы "неизвестных неизвестных" (см. главу 2, если не забыл). Он прямо говорит: "Чувак, вот всё, что тебе надо знать. Остальное — не твое дело".

## **4.3 Абстракции (или как врать во благо)**

*Абстракция* — это слово, которым любят бросаться на собесах. По сути, **абстракция — это упрощенный вид сущности, где мы выкинули всё ненужное говно**. Она нужна, чтобы наш мозг не взорвался от перегруза.

В коде интерфейс — это и есть абстракция. Мы показываем суть, а детали реализации прячем, потому что они *не важны* для использования.

Но тут главное слово — "неважные". Чем больше деталей выкинешь, тем лучше. Но если ты выкинешь что-то реально важное — получится **ложная абстракция**. Это когда с виду всё просто, а начинаешь юзать — и получаешь неожиданный эффект (например, тормоза или потерю данных).

Пример: файловая система. Тебе насрать, на какой сектор диска записался байт. Это удачная абстракция. Но если ты пишешь базу данных, тебе важно знать, когда данные реально упали на диск, а не зависли в кэше, чтобы при выдергивании шнура из розетки не проебать транзакции. Если файловая система скрывает от тебя управление сбросом кэша — это хуевая абстракция для твоей задачи.

В жизни так же: микроволновка — это абстракция. Ты жмешь кнопку "Греть", а не настраиваешь частоту магнетрона. Машина — абстракция. Ты крутишь руль, а не регулируешь впрыск топлива в цилиндры вручную.

## **4.4 Глубокие модули (Deep Modules) — наш бро**

Лучшие модули — **глубокие**. Это те, которые дают дохера пользы через маленький, сука, простой интерфейс. Представь прямоугольник (рис. 4.1). Площадь — это функциональность. Верхняя грань — это интерфейс.
Глубокий модуль: маленькая грань сверху, огромная площадь внутри. Ты дергаешь одну ручку, а под капотом работает целый завод.

![](./figures/00012.jpeg)
*Рис 4.1: Слева — нормальный модуль здорового человека (Глубокий). Справа — модуль курильщика (Мелкий/Поверхностный).*

Глубина — это соотношение цены и качества. Цена — это сложность интерфейса (то, что надо выучить). Польза — это то, что модуль делает. Хороший модуль: **дешевый интерфейс, дорогая функциональность**.

Пример для подражания: **Unix I/O**.
Всего 5 основных функций: `open`, `read`, `write`, `lseek`, `close`. Сигнатуры простейшие.
А под капотом? Сотни тысяч строк кода! Файловые системы, кэширование, права доступа, работа с железом, очереди, прерывания. Всё это дерьмо скрыто от тебя за пятью функциями. Это, блять, гениально.

Еще пример: **Сборщик мусора (Garbage Collector)** в Go или Java. У него вообще нет интерфейса! Он молча ходит и убирает за тобой говно. Идеальная абстракция: ноль когнитивной нагрузки, максимум пользы.

## **4.5 Мелкие модули (Shallow Modules) — бесполезная херня**

А вот это — рак современной разработки. **Мелкий модуль** — это когда интерфейс сложный, а внутри нихера толком не происходит.
Пример: класс связанного списка (`LinkedList`). Чтобы вставить элемент, надо пару строк кода. Класс не скрывает почти никакой сложности. Интерфейс почти равен реализации. Иногда без них никак, но сложности они не убавляют.

Экстремальный пример говнокода от студентов:
```java
private void addNullValueForAttribute(String attribute) {
    data.put(attribute, null);
}
```
Этот метод — **лютый мусор**. Он добавляет сложности (надо знать про этот метод, помнить его имя), но не дает никакой абстракции. Ты тупо пишешь больше букв, чем если бы вызвал `data.put` напрямую.

🚩 **Красный флаг: Мелкий модуль** 🚩
Если тебе приходится учить интерфейс модуля дольше, чем писать эту функциональность самому — это мелкий модуль. Мелкие модули не помогают бороться со сложностью, они её плодят.

## **4.6 "Классит" (Classitis): Болезнь плодить классы на каждый чих**

К сожалению, про глубокие модули сейчас забыли. Сейчас в моде догма: "Классы должны быть маленькими". Студентов и джунов учат: "Если метод больше N строк — режь его!".
Это приводит к болезни, которую я называю **Classitis (Классит)**.

Разрабы начинают дробить код на миллиард микро-классов. Им кажется: "О, много маленьких классов — это же хорошо!". Нет, блять, это не хорошо!
Да, каждый класс по отдельности простой. Но сложность системы переезжает на уровень **взаимодействия** между этими огрызками. У тебя куча интерфейсов, куча переключений контекста, и чтобы понять, как работает простая фича, тебе надо продраться через 50 файлов. Это "смерть от тысячи порезов".

## **4.7 Примеры: Java (как не надо) и Unix I/O (как надо)**

Яркий пример "Классита" — библиотека ввода-вывода в Java. Там, видимо, решили поиздеваться над людьми.
Чтобы просто прочитать файл с буферизацией (что нужно в 99% случаев), тебе надо сплясать с бубном:

```java
FileInputStream fileStream = new FileInputStream(fileName);
BufferedInputStream bufferedStream = new BufferedInputStream(fileStream);
ObjectInputStream objectStream = new ObjectInputStream(bufferedStream);
```

Три, сука, объекта! `FileInputStream` тупой и не умеет в буфер. `BufferedInputStream` добавляет буфер. `ObjectInputStream` добавляет чтение объектов.
Первые два объекта становятся мусором сразу после создания третьего.

Разрабы Java скажут: "Ну мы же дали гибкость! Вдруг кто-то захочет читать без буфера?".
Да идите вы в жопу с такой гибкостью. **Интерфейс должен делать самый частый сценарий максимально простым**. Почти всем нужен буфер. Сделайте его по дефолту! А для полутора землекопов, которым буфер не нужен, сделайте отдельную настройку где-нибудь в глубине.

Сравните с Unix: там буферизация включена по умолчанию. Разрабы Unix понимали, что никто не хочет тормозов. Они сделали глубокий модуль. А в Java сделали конструктор "Собери себе читалку сам из говна и палок".

## **4.8 Итог**

Разделяй интерфейс и реализацию. Прячь сложность в кишках.
Главное правило: **Делай модули глубокими**.
Интерфейс должен быть простым для обычных задач, но функционал должен быть мощным.
Не плоди мелкие классы без нужды. Не будь как Java-архитектор.

*P.S. Ученые пытаются придумать языки, где интерфейс описывается формально и полностью. Но пока что обычный английский (или русский мат) в комментах работает лучше.*