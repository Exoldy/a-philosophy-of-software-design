# **7. Разные слои — разные абстракции**

Программные системы строятся слоями. Верхние едут на нижних. В нормально спроектированной системе каждый слой предлагает **новую абстракцию**, отличную от того, что творится этажом ниже. Если проследить путь вызова метода сверху вниз, абстракции должны меняться, сука, кардинально.

**Примеры для закрепления:**
*   **Файловая система:** Наверху у тебя «Файл» — массив байтов, пиши-читай. Чуть ниже — кэш в памяти (фиксированные блоки). Ещё ниже — драйверы, которые пинают железо. Абстракции разные? Разные.
*   **TCP:** Наверху — надежный поток байтов. Внизу — пакеты, которые летают как попало, теряются и приходят не в том порядке. Магия превращения хаоса в порядок — это и есть работа слоя.

**Суть проблемы:**
Если соседние слои выглядят одинаково — это **ебаный красный флаг**. Это значит, что вы обосрались с декомпозицией классов.

---

## **7.1 Методы-курьеры (Pass-through methods): "Я просто передам это дальше" (Бесполезная херня)**

Это классика говнокода. Когда соседние слои делают одно и то же, появляются они — методы-прокладки. Это такой метод, который нихуя не делает, кроме как вызывает другой метод с почти такой же сигнатурой.

Пример из жизни (проект студента, текстовый редактор):

```java
public class TextDocument ... {
    private TextArea textArea;
    ...
    public void insertString(String textToInsert, int offset) {
        textArea.insertString(textToInsert, offset);
    }
    ...
}
```

В этом классе 13 из 15 методов были тупо прокладками.

**🚩 Красный флаг: Метод-прокладка**
Если метод просто передает свои аргументы другому методу — это симптом. Это значит, что разделение ответственности между классами проёбано.

Такие методы делают классы «мелкими» (shallow). Интерфейс раздувается, сложности добавляется, а функциональности — **ноль целых хрен десятых**. Это просто бюрократия в коде. Плюс ты создаешь жесткую зависимость: поменял сигнатуру внизу — иди меняй и вверху.

**Что делать?**
Рефакторить к чертям.
1.  Пусть вызывающий код дергает нижний класс напрямую (убираем посредника).
2.  Перераспредели функционал, чтобы верхний класс делал хоть что-то полезное.
3.  Смерджи эти два класса, раз уж они такие неразлучные.

![](./figures/00015.jpeg)

*Рис 7.1: Методы-прокладки. В варианте (a) класс C1 содержит pass-through методы. В (b) вызывающий код обращается к C2 напрямую. В (c) функционал перераспределён. В (d) классы объединены.*

## **7.2 Когда дублировать интерфейс — это ок? (Спойлер: редко)**

Иногда иметь одинаковые сигнатуры — не зашквар. Главное, чтобы каждый метод приносил **реальную пользу**.

**Пример: Диспетчер.**
Сервер получает HTTP-запрос и решает, кому его отдать. Сигнатуры у обработчиков одинаковые, но логика внутри разная. Диспетчер делает полезную работу — он разруливает трафик.

**Пример: Драйверы.**
У всех дисков один интерфейс. Это снижает когнитивную нагрузку. Ты один раз выучил, как работать с диском, и тебе похер, SSD там или крутящийся блин.

## **7.3 Декораторы (Decorators): Паттерн, которым вы злоупотребляете**

Паттерн «Декоратор» — это спонсор дублирования API. Ты берешь объект, оборачиваешь его в другой объект с тем же интерфейсом, но добавляешь какую-то финтифлюшку.

Классический пример — **Java I/O**. `BufferedInputStream` оборачивает `InputStream`.
Идея благородная: отделить буферизацию от чтения.
Реальность: **Тонны бойлерплейта**.

Декораторы часто превращаются в пустышки с кучей pass-through методов. Разработчики начинают плодить классы на каждый чих.

**Прежде чем пилить декоратор, спроси себя:**
*   Может, просто добавить эту фичу в базовый класс? (Серьезно, кому нахер нужен небуферизированный ввод в 2024 году? Просто объедините их, блять).
*   Если фича нужна только для одного кейса, может, впилить её прямо в юзкейс?
*   Может, слить её с другим декоратором, чтобы не плодить матрешку из объектов?

## **7.4 Интерфейс vs Реализация: Вечная битва**

Ещё одно применение правила «разные слои — разные абстракции»: **Интерфейс класса должен отличаться от его кишок**.

Если у тебя внутри данные хранятся как список строк, и наружу ты отдаешь методы `getLine` и `putLine` — ты написал **мелкий** (shallow) класс. Ты заставляешь пользователя твоего класса думать о твоих внутренних проблемах. Если юзеру надо вставить текст посередине, ему придется самому резать строки, склеивать их и страдать.

**Как надо:**
Внутри храни хоть в строках, хоть в байтах, но наружу дай нормальный метод `insert(text, position)`. Спрячь сложность нарезки строк внутрь. Пусть класс потеет, а не тот, кто его вызывает.

## **7.5 Переменные-курьеры: Передаем мусор по цепочке вызовов**

Это переменные, которые передаются через цепочку методов, как горячая картошка, только чтобы попасть в самый низ.

Пример: `main()` создает сертификат, который нужен методу `m3()` глубоко в жопе стека вызовов. И этот несчастный сертификат тащится аргументом через `m1()` и `m2()`, которым он нахер не упал.

**Почему это пиздец:**
Все промежуточные методы загрязняются ненужным знанием. Захочешь добавить новую опцию — придется переписывать сигнатуры у половины проекта.

**Как лечить:**
1.  **Глобальные переменные?** Вариант для самоубийц. Проблемы с тестами и многопоточкой обеспечены.
2.  **Контекст (Context Object).** Самое рабочее решение, хоть и не идеальное.

**Контекст** — это такой объект-помойка (в хорошем смысле), где лежит всё глобальное состояние: конфиги, таймауты, шареные ресурсы. Ты создаешь его в начале и передаешь либо в конструкторы, либо хранишь ссылку.

![](./figures/00016.gif)

*Рис 7.2: Способы борьбы с переменными-курьерами. (a) cert передаётся через m1 и m2, хотя им он не нужен. (b) main и m3 шарят объект. (c) Глобальная переменная. (d) Контекст-объект, ссылка на который хранится в нужных классах.*

Да, контекст похож на глобальные переменные, но он хотя бы локализован в одном объекте. Это "узаконенное зло". Но лучше так, чем прокидывать 10 аргументов через 20 методов.

## **7.6 Итог**

Каждый новый класс, метод или переменная — это **налог на сложность**. Если ты добавляешь что-то в архитектуру, оно должно окупать свое существование, упрощая жизнь в другом месте.

Если слои имеют одинаковые абстракции (прокладки, тупые декораторы) — значит, ты просто усложнил систему, не получив нихуя взамен. Не делай так.