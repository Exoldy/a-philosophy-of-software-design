# **9. Вместе или порознь? (Сплитить или мержить?)**

Один из самых фундаментальных вопросов, от которого у тебя будут бессонные ночи: **вот есть два куска функционала — слить их в экстазе в один модуль или разнести по разным углам?**

Этот вопрос всплывает везде: в функциях, методах, классах, микросервисах. Например, должен ли класс для работы с файлами сам заниматься буферизацией, или пусть этим занимается кто-то другой? Парсинг HTTP-запроса — это один жирный метод или десять мелких, размазанных по трем классам?

В этой главе мы разберем, как принимать эти решения и не облажаться.

Главная цель, как всегда, — **уменьшить сложность**. Казалось бы, логика подсказывает: "Нарежь всё на мелкие кусочки, маленькое проще понять". **Хер там плавал.** Дробление системы само по себе порождает сложность, которой раньше не было:

1.  **Сложность количества:** Чем больше у тебя компонентов, тем сложнее удержать эту оргию в голове. Найти нужный кусок кода в проекте из 1000 микро-файлов — тот еще квест.
2.  **Сложность управления:** Теперь тебе нужен код, который будет дирижировать этим зоопарком. Раньше у тебя был один объект, теперь ты жонглируешь пятью.
3.  **Сложность разделения:** Если код разнесли по разным файлам, тебе придется скакать между ними как горный козел. Если компоненты реально независимы — ок, это благо. Но если между ними есть неявные связи (а они, сука, всегда есть), ты будешь переключать вкладки IDE, пока не случится эпилептический припадок.
4.  **Дублирование:** То, что раньше лежало в одном месте, теперь придется копипастить в каждый "независимый" кусок.

---

### **Когда надо объединять (Признаки родственных душ)**

Склеивать код полезно, если куски тесно связаны. Если они друг другу никто — пусть живут раздельно. Вот признаки того, что коду пора съехаться:

*   **Они делят информацию.** Оба куска зависят, например, от синтаксиса одного и того же документа.
*   **Они используются вместе.** Типа "мы с Тамарой ходим парой". Это работает, только если связь двусторонняя. (Пример: кэш диска всегда юзает хеш-таблицу, но хеш-таблица может жить и без кэша. Значит, хеш-таблица должна быть отдельным модулем, не смешивай).
*   **Концептуальное перекрытие.** Оба куска делают одну общую задачу (например, поиск подстроки и смена регистра — это все "манипуляции со строками").
*   **Нельзя понять один кусок без другого.** Если ты читаешь метод А, но ни хера не понимаешь, пока не прочитаешь метод Б — поздравляю, вы их зря разделили.

---

## **9.1 Инфа общая? Объединяй, не тупи**

Вспомним пример с HTTP-сервером (из главы 5). Сначала там было два метода: один читал байты из сокета в строку, второй парсил эту строку.
Звучит логично? **Нет.**
Потому что первый метод ("читатель") ни хера не мог нормально прочитать запрос, не понимая его структуру (например, где конец заголовков). В итоге "читатель" занимался парсингом, чтобы просто прочитать, а "парсер" парсил еще раз.
**Решение:** Слить их нахер вместе. Код стал короче, проще, и никто не делает двойную работу.

## **9.2 Это упростит интерфейс? Объединяй**

Иногда два модуля — это просто две половинки одной жопы. Если их объединить, интерфейс станет чище.
В примере с HTTP, когда методы объединили, исчезла необходимость передавать сырую строку запроса от одного к другому. Интерфейс стал проще.

Еще пример: **Java I/O**. Если бы `FileInputStream` и `BufferedInputStream` были одним классом с буферизацией по умолчанию, миллионы программистов сказали бы спасибо. Сейчас же каждый раз приходится писать матрешку из конструкторов, чтобы просто прочитать файл и не умереть от тормозов. Объединение позволило бы сделать буферизацию "магической" — она просто работает, и пользователю похуй, как.

## **9.3 Дублируется код? Объединяй**

Если видишь копипасту — это рак. Лечится двумя способами:

1.  **Вынести в отдельный метод.** Работает, если кусок кода большой, а сигнатура нового метода простая. Если ты выносишь две строчки кода в метод, который принимает 10 аргументов по ссылке — ты делаешь только хуже.
2.  **Рефакторинг потока выполнения.** Представь: у тебя в методе 5 мест, где может вылезти ошибка, и в каждом надо сделать `cleanup()` перед выходом.
    *   *Вариант для смелых:* Использовать `goto`. Да-да, **GOTO**. Дейкстра, конечно, вертится в гробу, но переход на метку очистки в конце функции (как в ядре Linux) — это часто чище, чем дублировать код очистки или городить вложенные `try-catch-finally`.

## **9.4 Разделяй универсальное и специфичное (Мухи отдельно, котлеты отдельно)**

Золотое правило: **Не мешай мух с котлетами.**
Если модуль предоставляет общий механизм (например, текстовый редактор), не надо пихать туда код, специфичный для UI (например, "удалить выделение по клику мыши").
Общий механизм должен быть чистым. Специфика — слоем выше.

**🚩 RED FLAG: Смесь общего и частного**
Если в общем классе (General Purpose) появляются методы типа `handleUserClickOnTuesday()`, ты свернул не туда. Это называется "протечка абстракций" и ведет к тому, что при смене UI тебе придется переписывать ядро системы.

---

## **9.5 Пример: Курсор и Выделение текста**

В проекте редактора GUI были курсор (мигающая палка) и выделение (синяя область).
Студенты подумали: "О, курсор всегда на краю выделения! Давайте сделаем **ОДИН ОБЪЕКТ**, который управляет и тем, и другим!".

Получился **Франкенштейн**.
Вместо того чтобы просто хранить координаты, этот объект хранил какие-то флаги, булевы переменные "есть ли выделение", "с какой стороны курсор". Чтобы узнать позицию курсора, надо было распарсить состояние этого объекта.
**Итог:** Разделили обратно. Сделали простой класс `Position`. Курсор — это одна `Position`. Выделение — две `Position`. Всё. Простота победила.

---

## **9.6 Пример: Отдельный класс для логов (Нахрена?)**

Студент написал такой код:
```java
try { ... } catch (Exception e) {
    NetworkErrorLogger.logRpcOpenError(req, dest, e);
}
```
А внутри `NetworkErrorLogger`:
```java
public static void logRpcOpenError(...) {
    logger.log(Level.WARNING, "Всё сломалось: " + ...);
}
```
Чувак создал целый класс и кучу методов просто чтобы... вызвать другой логгер.
Это **методы-пустышки (shallow methods)**. Они добавляют зависимости, заставляют тебя прыгать по файлам, требуют документации, но не делают ни-че-го полезного.
**Решение:** Убей этот класс. Пиши лог прямо в `catch`. Читать станет легче.

---

## **9.7 Пример: Механизм Undo (Отмена действий — это боль)**

Задача: сделать Ctrl+Z для текста, выделения и прокрутки.
Плохой вариант: Запихать всё в класс `Text`.
В итоге класс `Text` знает про UI, про скролл, про выделение. Он превращается в помойку.

**Хороший вариант:**
Выделить **общий механизм** истории (`History`).
Класс `History` тупой. Он не знает, *что* он отменяет. Он просто хранит список объектов с интерфейсом `Action` (у которых есть методы `undo()` и `redo()`).
*   Класс `Text` создает экшн `TextInsertAction`.
*   UI создает экшн `SelectionChangeAction`.
*   `History` просто дергает их за ниточки.

Разделение ответственности:
1.  `History` — управляет списком (общий механизм).
2.  `Action` — знают, как отменить конкретную херню (специфика).
3.  UI — решает, когда группировать действия (политика).

---

## **9.8 Разделение и слияние методов**

Существует миф: *"Метод не должен быть длиннее 20 строк! Если больше — режь!"*.
**Это бред сивой кобылы.** Длина метода сама по себе ничего не значит.

Разбивать метод на мелкие куски часто вредно:
*   Ты плодишь лишние интерфейсы.
*   Ты разрываешь логику. Чтобы понять, что происходит, приходится собирать пазл из 10 микро-методов.

**Длинные методы — это норм**, если:
1.  У них простая сигнатура (мало аргументов).
2.  Они читаются сверху вниз как история.
3.  Они "глубокие" (делают много полезного за простым интерфейсом).

**Когда реально надо дробить:**
1.  **Выделение подзадачи.** Если внутри метода есть кусок, который решает отдельную, изолированную задачу (и не требует доступа ко всем локальным переменным родителя) — выноси.
2.  **Разделение функционала.** Если метод пытается усидеть на двух стульях (делает две не связанные вещи) — раздели на два независимых метода.

**🚩 RED FLAG: Сиамские близнецы (Conjoined Methods)**
Если ты разбил метод на два, но чтобы понять первый, надо обязательно прочитать второй — ты сделал херню. Ты не упростил систему, ты просто размазал сложность тонким слоем.

---

## **9.9 Итог**

Решение "слить или разделить" принимается не по линейке и не по количеству строк.
Смотри на **сложность**.
Выбирай структуру, которая:
1.  Лучше прячет инфу (Information Hiding).
2.  Создает меньше зависимостей.
3.  Делает интерфейсы глубокими, а не широкими.

Если разделение заставляет тебя страдать — **склеивай обратно**.