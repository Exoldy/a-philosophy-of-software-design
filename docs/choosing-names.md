# **14. Нейминг (Самая сложная проблема в Computer Science)**

Выбор имен для переменных, методов и прочей херни — это, пожалуй, самый недооцененный аспект разработки. Хорошее имя — это, сука, лучшая документация: оно делает код понятным даже для дебилов. Оно избавляет от необходимости писать тонны комментов и помогает ловить баги.

А вот хреновые имена — это прямой путь в ад сложности, двусмысленности и непоняток, которые потом вылезают боком на проде. Выбор имени — отличный пример того, как сложность накапливается по чуть-чуть. Если ты назовешь одну переменную "так себе", проект, может, и не развалится. Но в софте тысячи переменных. Если забить болт на все, ты получишь неподдерживаемый кусок говна.

## **14.1 Пример: Как кривое имя создает баги и дыры в безопасности**

Иногда даже одна переменная с ублюдским именем может положить всё. Самый лютый баг в моей карьере случился именно из-за этого.

В конце 80-х мы с аспирантами пилили распределенную ОС под названием Sprite. И тут заметили фигню: файлы иногда теряли данные. Один из блоков файла внезапно становился забитым нулями, хотя юзер его не трогал. Проблема всплывала редко, хрен отловишь. Студенты потыкались, ничего не нашли и сдались. Но я считаю любой неисправленный баг личным оскорблением и плевком в душу, поэтому решил докопаться до истины.

Полгода, Карл! Полгода я искал эту дрянь. А ларчик открывался просто (как и всегда, когда уже знаешь ответ).

В коде файловой системы была переменная с гениальным именем `block`. И использовалась она для двух разных вещей. В одном месте `block` означал **физический** номер блока на диске. В другом — **логический** номер блока в файле. К несчастью, в одном куске кода переменная `block` хранила логический номер, но её случайно сунули туда, где ожидался физический. В итоге левый сектор на диске затирался нулями.

Пока мы искали баг, куча народу (включая меня) смотрела в этот код и в упор не видела проблемы. Видишь `block` там, где нужен физический блок? Мозг на автомате думает: "Ну, тут лежит физический блок". Мне пришлось обвешать код логами с ног до головы, чтобы понять, где именно происходит коррупция данных, и пробить этот ментальный блок.

Если бы мы назвали переменные нормально — например, `fileBlock` и `diskBlock` — хрен бы кто ошибся. Компилятор или здравый смысл сразу бы дали по рукам.

К сожалению, большинство разрабов вообще не включают мозг, когда называют переменные. Пишут первое, что пришло в голову, лишь бы было "ну типа похоже". `block` — это же "типа похоже" и на то, и на другое? Значит, сойдет. **Нет, блять, не сойдет.** Это стоило нам полугода отладки. Не соглашайся на "сойдет". Потрать лишние 5 секунд, выбери точное, однозначное имя. Это окупится быстрее, чем ты думаешь.

## **14.2 Рисуй картинку в голове (Создавай образ, а не абстракцию)**

Цель имени — создать в голове читающего образ. Хорошее имя сразу говорит, чем эта хреновина является, а чем — нет.

Спроси себя: "Если какой-то левый чувак увидит это имя без контекста и документации, он догадается, что это за зверь?". Конечно, в одно имя "Войну и мир" не запихнешь, больше трех слов — это уже перебор. Челлендж в том, чтобы в пару слов упаковать самую суть.

Имена — это абстракция. Самые крутые имена фокусируют внимание на главном и скрывают ненужное дерьмо.

## **14.3 Имя должно быть точным (Precise)**

У хороших имен два свойства: точность и последовательность.

Начнем с точности. Главная беда — имена слишком общие или размытые. Читатель начинает додумывать и, как правило, додумывает неправильно (привет, баг с `block`).

Возьмем пример:
```java
/**
 * Возвращает общее количество indexlets, которыми управляет объект.
 */
int IndexletManager::getCount() {...}
```

`getCount` — это, блять, что? Количество чего? Если я вижу вызов этого метода, я без понятия, что он вернет, пока не полезу в доки. Назови ты его `getActiveIndexlets` или `numIndexlets` — и вопросов нет.

Вот еще примеры "шедевров" от моих студентов:

*   **`x` и `y` для позиции символа в тексте.**
    Слишком абстрактно. `x` и `y` — это обычно пиксели на экране. Если я вижу `x`, я не думаю про номер символа в строке. Назови `charIndex` и `lineIndex`, не путай людей.

*   **`blinkStatus` (Статус моргания).**
    ```java
    // Blink state: true when cursor visible.
    private boolean blinkStatus = true;
    ```
    Слово "status" — это мусор. Оно ничего не говорит. "Blink" тоже мутное — что там моргает? Глаз у тимлида?
    Лучше так:
    ```java
    private boolean cursorVisible = true;
    ```
    Сразу понятно: `true` — курсор видно, `false` — не видно. Имена булевых переменных должны звучать как вопрос или утверждение (предикат), а не как "статус чего-то там".

*   **`VOTED_FOR_SENTINEL_VALUE = "null"`**
    Означает, что сервер еще не голосовал. Название говорит, что значение "особенное", но не говорит, что оно значит. Назови `NOT_YET_VOTED`, ну ё-моё.

*   **Переменная `result` в void-методе.**
    Во-первых, это звучит так, будто это возвращаемое значение. Во-вторых, "результат" чего? Анализов? Назови конкретно: `mergedLine`, `totalChars`. Использовать `result` можно только в очень коротких методах, где и так всё ясно, и это реально то, что вернется через `return`.

🚩 **Красный флаг: Мутное имя**
Если имя переменной настолько широкое, что под него подходит половина вселенной, значит, оно не несет никакой инфы.

Есть исключения. `i` и `j` в циклах — это святое, но только если цикл на три строчки. Если цикл на три экрана, и ты используешь `i`, ты садист.

Бывает и перегиб с точностью:
```java
void delete(Range selection) {...}
```
Аргумент назвали `selection` (выделение), но метод работает с любым диапазоном, не обязательно выделенным юзером. Назови просто `range`.

Если ты сидишь и тупишь, не в силах придумать нормальное имя — это **сигнал**. Скорее всего, у тебя в коде каша, и одна переменная отвечает за всё сразу (и жнец, и швец, и на дуде игрец). Разбей её на несколько, и имена придут сами.

🚩 **Красный флаг: Хрен придумаешь имя**
Трудно назвать? Значит, дизайн — говно. Рефактори.

## **14.4 Последовательность (Consistency) — наше всё**

В любом проекте есть сущности, которые используются везде. Если ты назвал переменную `fileBlock` в одном месте, не надо в другом называть её `blockIndex` или `fBlock`. Будь последователен. Это снижает когнитивную нагрузку: увидел знакомое название — сразу понял, о чем речь.

Три правила:
1. Используй одно имя для одной цели.
2. Не используй это имя для других целей.
3. Убедись, что цель достаточно узкая. (В примере с багом `block` нарушили третье правило — запихнули туда два разных понятия).

Если нужно две одинаковые штуки (например, копируем данные), используй префиксы: `srcFileBlock` и `dstFileBlock`.

И с циклами: `i` — внешний, `j` — вложенный. Если я увижу `j` во внешнем цикле, я подумаю, что ты наркоман.

## **14.5 Отдельное мнение: Go Style Guide (Там свои приколы с короткими именами)**

Не все согласны с моим подходом. Разрабы языка Go, например, считают, что имена должны быть короткими, желательно из одной буквы. Эндрю Герранд из команды Go заявляет, что "длинные имена скрывают суть кода".

Он приводит пример с короткими именами:
```go
func RuneCount(b []byte) int {
    i, n := 0, 0
    for i < len(b) {
        if b[i] < RuneSelf {
            i++
        } else {
            _, size := DecodeRune(b[i:])
            i += size
        }
        n++
    }
    return n
}
```

И утверждает, что это читабельнее, чем вот это:
```go
func RuneCount(buffer []byte) int {
    index, count := 0, 0
    for index < len(buffer) {
        if buffer[index] < RuneSelf {
            index++
        } else {
            _, size := DecodeRune(buffer[index:])
            index += size
        }
        count++
    }
    return count
}
```

Лично я в душе не чаю, чем второй вариант сложнее первого. Зато слово `count` сразу говорит мне, что это счетчик. А `n`? Это может быть что угодно. Мне пришлось читать весь код, чтобы понять, что такое `n`.

В "культуре Go" принято использовать одни и те же короткие имена для кучи всего: `c` — это клиент? Счетчик? Канал? `d` — это данные? Дистанция? Разница? Дебильность?
Такая двусмысленность — это прямой путь к багам, как в моем примере с `block`.

Короче, читабельность определяется тем, кто **читает**, а не тем, кто пишет. Если тебе ок, и команде ок — пиши хоть однобуквенно. Но если люди жалуются, что твой код похож на шифр Энигмы — пиши нормально.

И да, Герранд прав в одном: чем дальше использование переменной от её объявления, тем длиннее должно быть имя.

## **14.6 Итог**

Хорошие имена делают код очевидным. Читатель делает предположение о том, что делает переменная, и — о чудо! — оказывается прав. Это инвестиция. Потрать время сейчас, чтобы не ебаться с этим потом.

Сначала будет сложно и бесяче подбирать идеальные имена. Но со временем прокачаешься, и будешь выдавать их на автомате, получая профит практически нахаляву.